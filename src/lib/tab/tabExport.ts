// The tab staff PDF export shares the diagram exporter (jspdf + svg2pdf.js,
// A4 by default, auto orientation). Re-exported here so existing imports keep
// working.
export { downloadPdfFromContainer } from "@/lib/scaleExport";
